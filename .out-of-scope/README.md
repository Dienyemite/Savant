# Out-of-scope features

This directory stores persistent records of rejected feature requests — institutional memory for why something was decided against, so it isn't re-litigated when a similar request comes in later.

See the engineering skill docs for how to read and write these files during triage.
